// constants
let initialState = {
    
}

// reducer
const reducer = (state= initialState, action) => {
    switch (action.type) {
        
        default:
            return state
    }
}

// action creator


export default reducer